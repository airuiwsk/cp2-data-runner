name: XL001 DefiLlama Parse Probe

on:
  push:
    paths:
      - "xl001_defillama_parse_probe.py"
      - ".github/workflows/xl001-defillama-parse-probe.yml"
      - "XL001_PARSE_TRIGGER.md"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Probe structured public records
        run: python xl001_defillama_parse_probe.py
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: xl001-defillama-parse-probe
          path: xl001-defillama-parse-probe.json
          if-no-files-found: error
